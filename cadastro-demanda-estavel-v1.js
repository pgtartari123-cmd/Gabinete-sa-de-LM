/* GABINETE LM — CADASTRO/DEMANDA ESTÁVEL v2
   Mantém o formulário atual. Normalização é idempotente: não altera timestamp a cada ciclo.
   Nunca apaga o cadastro existente.
*/
(function(){'use strict';
const DB='gabineteDigitalDemo';
const uid=()=>typeof crypto!=='undefined'&&crypto.randomUUID?crypto.randomUUID():('xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g,c=>{const r=Math.random()*16|0,v=c==='x'?r:(r&3|8);return v.toString(16)}));
const read=()=>{try{const d=JSON.parse(localStorage.getItem(DB)||'{"people":[],"agenda":[]}');d.people=Array.isArray(d.people)?d.people:[];d.agenda=Array.isArray(d.agenda)?d.agenda:[];return d}catch(e){return{people:[],agenda:[]}}};
const write=d=>localStorage.setItem(DB,JSON.stringify(d));
const now=()=>new Date().toISOString();
function normalDemand(x,p){x=x||{};const oldUpdated=x.atualizadoEm||x.criadoEm||p.atualizadoEm||p.criadoEm||now(), oldCreated=x.criadoEm||p.criadoEm||now();return {id:x.id||uid(),demanda:String(x.demanda||p.demanda||''),tipoDemanda:String(x.tipoDemanda||x.tipo||p.tipoDemanda||p.tipo||'Outro'),tipo:String(x.tipo||x.tipoDemanda||p.tipoDemanda||p.tipo||'Outro'),procedimento:String(x.procedimento||p.procedimento||''),status:['Pendente','Em andamento','Concluído'].includes(x.status)?x.status:(p.status||'Pendente'),observacoes:String(x.observacoes||p.observacoes||''),prioridade:x.prioridade||'Normal',responsavel:x.responsavel||'',prazo:x.prazo||'',encaminhamento:x.encaminhamento||'',retorno:x.retorno||'',secretaria:x.secretaria||'',anexos:Array.isArray(x.anexos)?x.anexos:[],protocolo:x.protocolo||'',destinoEnvio:String(x.destinoEnvio||p.destinoEnvio||''),dataSaida:x.dataSaida||p.dataSaida||'',dataAbertura:x.dataAbertura||p.dataAbertura||oldCreated,criadoEm:oldCreated,atualizadoEm:oldUpdated};}
function normalize(p){const antes=JSON.stringify(p);p.demandas=Array.isArray(p.demandas)?p.demandas:[];if(!p.demandas.length&&p.demanda)p.demandas=[normalDemand({},p)];p.demandas=p.demandas.map(x=>normalDemand(x,p));const last=p.demandas[p.demandas.length-1];if(last){p.demanda=last.demanda;p.tipoDemanda=last.tipoDemanda;p.tipo=last.tipo;p.procedimento=last.procedimento;p.status=last.status;p.destinoEnvio=last.destinoEnvio;p.dataSaida=last.dataSaida}p.atualizadoEm=p.atualizadoEm||p.criadoEm||now();return JSON.stringify(p)!==antes}
function instalar(){const f=document.getElementById('formCadastro');if(!f||f.dataset.demandaEstavel)return;f.dataset.demandaEstavel='1';f.addEventListener('submit',function(){setTimeout(()=>{try{const d=read();const nome=String(f.elements.nome?.value||'').trim();let p=d.people.find(x=>String(x.nome||'').trim()===nome);if(!p)return;normalize(p);const raw={};new FormData(f).forEach((v,k)=>raw[k]=String(v??'').trim());const nd=normalDemand(raw,p);const i=p.demandas.findIndex(x=>String(x.id)===String(p.demandaId));if(i>=0)p.demandas[i]=nd;else p.demandas.push(nd);p.demandaId=nd.id;p.demanda=nd.demanda;p.tipoDemanda=nd.tipoDemanda;p.tipo=nd.tipo;p.procedimento=nd.procedimento;p.status=nd.status;p.destinoEnvio=nd.destinoEnvio;p.atualizadoEm=now();write(d);if(window.GabineteDB?.sincronizar)setTimeout(()=>window.GabineteDB.sincronizar(),50)}catch(e){console.error('Cadastro demanda estável:',e)}},80)},{capture:true});}
function reparar(){try{const d=read();let mudou=false;d.people.forEach(p=>{if(normalize(p))mudou=true});if(mudou)write(d)}catch(e){console.warn(e)}}
window.GabineteLM_CadastroEstavel={reparar,normalizar:reparar};
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>{instalar();reparar()});else{instalar();reparar()}
setInterval(instalar,1500);
})();
