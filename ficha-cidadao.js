(function(){const esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));function getDB(){try{return JSON.parse(localStorage.getItem('gabineteDigitalDemo')||'{"people":[]}')}catch(e){return{people:[]}}}window.verFichaCidadao=function(id){const p=(getDB().people||[]).find(x=>x.id===id);if(!p)return alert('Cidadão não encontrado.');const ds=Array.isArray(p.demandas)?p.demandas:(p.demanda?[{id:'principal',demanda:p.demanda,tipoDemanda:p.tipoDemanda||p.tipo,procedimento:p.procedimento,status:p.status||'Pendente'}]:[]);let m=document.getElementById('fichaCidadao');if(!m){m=document.createElement('div');m.id='fichaCidadao';m.className='modal-ficha';document.body.appendChild(m)}m.innerHTML='<div class="ficha"><div class="ficha-topo"><div><span class="eyebrow">FICHA DO CIDADÃO</span><h2>'+esc(p.nome||'Sem nome')+'</h2></div><button type="button" class="fechar-ficha">✕</button></div><div class="ficha-grid"><div><small>CPF</small><strong>'+esc(p.cpf||'Não informado')+'</strong></div><div><small>Telefone / WhatsApp</small><strong>'+esc(p.telefone||'Não informado')+'</strong></div><div><small>Nascimento</small><strong>'+esc(p.nascimento||'Não informado')+'</strong></div><div><small>Cartão SUS</small><strong>'+esc(p.sus||'Não informado')+'</strong></div><div><small>Nome da mãe</small><strong>'+esc(p.mae||'Não informado')+'</strong></div><div><small>Bairro / Comunidade</small><strong>'+esc(p.bairro||'Não informado')+'</strong></div><div class="ficha-full"><small>Endereço</small><strong>'+esc(p.endereco||'Não informado')+'</strong></div></div><div class="ficha-secao"><div class="ficha-secao-titulo"><h3>Histórico de demandas</h3><span>'+ds.length+' demanda(s)</span></div>'+ds.map((d,i)=>'<div class="demanda-historico"><div><strong>#'+(i+1)+' — '+esc(d.demanda||'Demanda')+'</strong><p>'+esc(d.tipoDemanda||d.tipo||'Tipo não informado')+(d.procedimento?' • '+esc(d.procedimento):'')+'</p></div><span class="status '+(d.status==='Concluído'?'concluido':d.status==='Em andamento'?'andamento':'pendente')+'">'+esc(d.status||'Pendente')+'</span></div>').join('')+'</div><div class="ficha-acoes"><button type="button" id="fEditar">✏️ Editar</button><button type="button" id="fNova">+ Nova demanda</button>'+(p.telefone?'<button type="button" id="fWhats">📱 WhatsApp</button>':'')+'<button type="button" onclick="window.print()">🖨️ Imprimir</button></div></div>';m.querySelector('.fechar-ficha').onclick=()=>m.remove();m.querySelector('#fEditar').onclick=()=>{m.remove();window.editarCadastro?.(id)};m.querySelector('#fNova').onclick=()=>{m.remove();window.adicionarDemanda?.(id)};if(p.telefone)m.querySelector('#fWhats').onclick=()=>window.wa?.(p.telefone,'Olá, '+p.nome+'! Estou entrando em contato pelo Gabinete Digital.');m.onclick=e=>{if(e.target===m)m.remove()}};})();